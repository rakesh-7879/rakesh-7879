document.addEventListener("DOMContentLoaded",(function(){let typingSpan=120;function startWriting(divId){let divLength=document.getElementById(divId).innerHTML.length,spans=document.getElementById(divId).getElementsByTagName("span");typeWriter(spans),setInterval(typeWriter,120*divLength,spans)}function typeWriter(spans){let setTimeoutTime=0;for(const span of spans){let text=span.innerHTML;span.innerHTML="",setTimeout(writing,setTimeoutTime,span,text),setTimeoutTime+=120*text.length,console.log(setTimeoutTime)}}function writing(span,text,i=0){i<text.length&&(span.innerHTML+=text.charAt(i),i++,setTimeout(writing,120,span,text,i))}startWriting("header-type-writer")}));